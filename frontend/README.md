# Frontend - LeadForge CRM

React + Tailwind + Framer Motion frontend for the Mini CRM.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run preview`
- `npm run lint`

## Environment

Create `.env` from `.env.example` and fill Firebase web config values.

For complete setup instructions, see the root project guide in `../README.md`.
